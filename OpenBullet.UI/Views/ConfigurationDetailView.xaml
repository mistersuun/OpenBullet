<UserControl x:Class="OpenBullet.UI.Views.ConfigurationDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="clr-namespace:OpenBullet.UI.Controls">
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Configuration Header -->
        <materialDesign:Card Grid.Row="0" Margin="0,0,0,16" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Configuration Info -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Configuration Editor" 
                               FontSize="24" FontWeight="Bold" 
                               Margin="0,0,0,16"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Name -->
                        <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,16,0">
                            <TextBlock Text="Name" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <TextBox x:Name="NameTextBox" 
                                     Text="{Binding ConfigurationName, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                     materialDesign:HintAssist.Hint="Configuration Name"/>
                        </StackPanel>
                        
                        <!-- Category -->
                        <StackPanel Grid.Column="1" Grid.Row="0" Margin="0,0,16,0">
                            <TextBlock Text="Category" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <TextBox x:Name="CategoryTextBox" 
                                     Text="{Binding ConfigurationCategory, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                     materialDesign:HintAssist.Hint="Category"/>
                        </StackPanel>
                        
                        <!-- Author -->
                        <StackPanel Grid.Column="2" Grid.Row="0">
                            <TextBlock Text="Author" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <TextBox x:Name="AuthorTextBox" 
                                     Text="{Binding ConfigurationAuthor, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                     materialDesign:HintAssist.Hint="Author"/>
                        </StackPanel>
                        
                        <!-- Description -->
                        <StackPanel Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Margin="0,16,16,0">
                            <TextBlock Text="Description" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <TextBox x:Name="DescriptionTextBox" 
                                     Text="{Binding ConfigurationDescription, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                     materialDesign:HintAssist.Hint="Description"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     Height="60"
                                     VerticalScrollBarVisibility="Auto"/>
                        </StackPanel>
                        
                        <!-- Version -->
                        <StackPanel Grid.Column="2" Grid.Row="1" Margin="0,16,0,0">
                            <TextBlock Text="Version" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <TextBox x:Name="VersionTextBox" 
                                     Text="{Binding ConfigurationVersion, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                     materialDesign:HintAssist.Hint="Version"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
                
                <!-- Action Buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Button x:Name="SaveButton" 
                            Content="Save Configuration"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Command="{Binding SaveConfigurationCommand}"
                            Margin="0,0,8,0">
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="ContentSave" Margin="0,0,8,0"/>
                                    <TextBlock Text="Save"/>
                                </StackPanel>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>
                    
                    <Button x:Name="TestButton" 
                            Content="Test Script"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Command="{Binding TestScriptCommand}">
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Play" Margin="0,0,8,0"/>
                                    <TextBlock Text="Test"/>
                                </StackPanel>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- LoliScript Editor -->
        <materialDesign:Card Grid.Row="1" Padding="0">
            <controls:LoliScriptEditor x:Name="ScriptEditor"
                                       ScriptText="{Binding ConfigurationScript, UpdateSourceTrigger=PropertyChanged}"
                                       ScriptTextChanged="ScriptEditor_ScriptTextChanged"/>
        </materialDesign:Card>
    </Grid>
</UserControl>

<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- Default Dark Theme Colors -->
    <SolidColorBrush x:Key="BackgroundMain" Color="#222222"/>
    <SolidColorBrush x:Key="BackgroundSecondary" Color="#111111"/>
    <SolidColorBrush x:Key="ForegroundMain" Color="#DCDCDC"/>
    <SolidColorBrush x:Key="ForegroundGood" Color="#ADFF2F"/>
    <SolidColorBrush x:Key="ForegroundBad" Color="#FF6347"/>
    <SolidColorBrush x:Key="ForegroundCustom" Color="#FF8C00"/>
    <SolidColorBrush x:Key="ForegroundRetry" Color="#FFFF00"/>
    <SolidColorBrush x:Key="ForegroundToCheck" Color="#7FFFD4"/>
    <SolidColorBrush x:Key="ForegroundMenuSelected" Color="#1E90FF"/>
    
    <!-- Button Styles -->
    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource BackgroundSecondary}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundMain}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ForegroundMain}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="Margin" Value="2"/>
    </Style>
    
    <!-- TextBox Styles -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource BackgroundMain}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundMain}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ForegroundMain}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="3"/>
        <Setter Property="Margin" Value="2"/>
    </Style>
    
    <!-- TextBlock Styles -->
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource ForegroundMain}"/>
    </Style>
    
    <!-- Menu Styles -->
    <Style TargetType="Menu">
        <Setter Property="Background" Value="{StaticResource BackgroundSecondary}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundMain}"/>
    </Style>
    
    <Style TargetType="MenuItem">
        <Setter Property="Background" Value="{StaticResource BackgroundSecondary}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundMain}"/>
    </Style>
    
</ResourceDictionary>


